# Largo ATQ shadcn prototype

Standalone Angular prototype for the shadcn-style Largo ATQ dashboard.

This app uses local mock data in `src/app/dashboard.service.ts` and does not require the FastAPI backend.

## Run

```bash
npm install
npm start
```

Open `http://127.0.0.1:4300`.

## Build

```bash
npm run build
```
